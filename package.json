{
  "scripts": {
    "build": "tsc --project tsconfig.json",
    "format": "prettier --list-different --write ."
  },
  "devDependencies": {
    "prettier": "^2.8.8",
    "typescript": "^5.0.3"
  }
}
