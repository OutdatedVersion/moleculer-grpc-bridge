{
  "scripts": {
    "build": "tsc --project tsconfig.json",
    "format": "prettier --list-different --write ."
  },
  "engines": {
    "node": ">= 16"
  },
  "devDependencies": {
    "@types/node": "^18.16.3",
    "prettier": "^2.8.8",
    "typescript": "^5.0.3"
  }
}
